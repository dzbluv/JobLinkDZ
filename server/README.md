Server (backend) for the project.

Quick vstart:

```bash
cd server
npm install
npm run dev
```

API endpoints:
- `GET /api/health` — health check
- `GET /api/users` — example users list

Configure using `.env` (see `../.env.example`).
